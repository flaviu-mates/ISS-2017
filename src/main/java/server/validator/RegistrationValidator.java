package server.validator;

import domain.Registration;

public class RegistrationValidator implements IValidator<Registration> {
    public void validate(Registration entity) throws ValidatorException {
        return;
    }
}