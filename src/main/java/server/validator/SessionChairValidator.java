package server.validator;

import domain.SessionChair;

public class SessionChairValidator implements IValidator<SessionChair> {
    public void validate(SessionChair entity) throws ValidatorException {
        return;
    }
}