package server.validator;

import domain.Session;

public class SessionValidator implements IValidator<Session> {
    @Override
    public void validate(Session entity) throws ValidatorException {
        return;
    }
}
